{
  "name": "faction-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute faction-db --file=./schema.sql --remote",
    "db:init-local": "wrangler d1 execute faction-db --file=./schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^4.107.1",
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
